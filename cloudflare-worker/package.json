{
  "name": "crackpacks-card-search-worker",
  "version": "2.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put POKEMON_TCG_API_KEY",
    "secret:ebay-client-id": "wrangler secret put EBAY_CLIENT_ID",
    "secret:ebay-client-secret": "wrangler secret put EBAY_CLIENT_SECRET",
    "check": "node --check src/index.js",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
